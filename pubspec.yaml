name: nolatech_messenger
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  cloud_firestore: ^5.6.9
  dartz: ^0.10.1
  firebase_auth: ^5.6.0
  firebase_core: ^3.14.0
  firebase_database: ^11.3.7
  flutter:
    sdk: flutter
  get_it: ^8.0.3
  go_router: ^15.2.0
  intl: ^0.20.2
  provider: ^6.1.5
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mockito: ^5.3.2
  build_runner:

flutter:
  uses-material-design: true
